Yahtzee Coach v43B Phase 2K.4 — FULL CURRENT APP deployment

This package REPLACES the Phase 2K.3.2 ZIP.

ONE-TIME SUPABASE STEP — DO THIS FIRST
1. Open Supabase -> SQL Editor.
2. Start a NEW query. Do not overwrite your earlier migration queries.
3. Copy/paste and run: RUN_THIS_ONCE_IN_SUPABASE_Phase2K4.sql
4. Confirm the query succeeds. This only adds the remembered-device session table/indexes/security.

UPLOAD TO GITHUB
1. Open the UPLOAD_TO_GITHUB folder from the release ZIP.
2. In GitHub, open the yahtzee-coach repository root.
3. Choose Add file -> Upload files.
4. Select ALL files inside UPLOAD_TO_GITHUB and upload them together.
5. Commit directly to main.

COPY/PASTE COMMIT MESSAGE:
v43B Phase 2K.4 - add 30-day remembered login and speed up Supabase reads

6. Wait for Streamlit Community Cloud to redeploy.

MORNING LIVE TEST
1. Sign in normally with Keep me signed in on this device for 30 days checked.
2. Fully close the browser tab/Home Screen app.
3. Reopen Yahtzee Coach. You should be signed in automatically.
4. Press Sign out, fully close, and reopen. You should remain signed out.
5. Open the Daily home and completed leaderboard and see whether the warm-app load feels faster.

EXPECTED PHASE 2K.4 BEHAVIOR
- Existing display name + PIN accounts continue to work unchanged.
- The PIN is never stored in the browser cookie.
- Remembered login lasts up to 30 days on that device.
- Sign out revokes the token and clears the cookie.
- Friend groups / leaderboards / question stats / Daily streak use batched Supabase reads.
- Short read-only caches reduce duplicate Supabase traffic during Streamlit reruns.
- Phase 2K.3.2 coaching language and dark-mode review fixes remain intact.

IMPORTANT
- Run the Phase 2K.4 SQL migration before expecting Remember Me to work.
- This patch does not change exact strategy, EV values, Daily puzzles, Practice generation, or scoring.
- Free-host cold starts can still happen; this patch targets avoidable app/database latency and repeated manual login.
- __pycache__ and .pyc files are intentionally excluded from the release.
