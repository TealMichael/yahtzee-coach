Yahtzee Coach v43B Phase 2H — FULL CURRENT APP deployment

This release contains the complete current Yahtzee Coach repository.
There is NO new Supabase migration for this phase.

STEP 1 — UPLOAD THE FULL CURRENT APP TO GITHUB
1. Open the UPLOAD_TO_GITHUB folder.
2. In GitHub, open the yahtzee-coach repository root.
3. Choose Add file -> Upload files.
4. Select ALL files inside UPLOAD_TO_GITHUB and upload them together.
5. Commit directly to main.

COPY/PASTE COMMIT MESSAGE:
v43B Phase 2H - fix install button and show device-specific steps

6. Wait for Streamlit Community Cloud to redeploy.
7. Open the normal app.

EXPECTED PHASE 2H BEHAVIOR
- Tapping the green home-screen/install button always produces a visible result.
- If a browser-native install prompt is available, the button opens it.
- Otherwise the app opens clear instructions directly underneath the button.
- iPhone/iPad instructions show Share -> Add to Home Screen -> Open as Web App -> Add.
- Android instructions show the Chrome menu path.
- Desktop Chrome instructions show Cast, save, and share -> Install page as app.
- Mac Safari instructions also show File -> Add to Dock.
- The custom mascot icon, Copy app link, Daily sharing, friend groups, and persistent Daily remain intact.

OPTIONAL DATABASE PREFLIGHT
https://teals-yahtzee-coach.streamlit.app/?dbcheck=1
Expected: v43B Phase 2H database preflight + green connection message.

IMPORTANT
- No Supabase SQL changes are needed for this phase.
- iPhone/iPad requires the user to make the final Apple-controlled Home Screen taps.
- Browser menus cannot be opened by the web page if the browser does not expose a native install prompt.
- The exact solver, exact policy, Daily puzzle bank, and Practice strategy behavior are unchanged.
- __pycache__ and .pyc files are intentionally excluded from the release.
